QuickbooksLibraryInstaller
==========================

This is the installer project used for Eaglesoft Quickbooks tool. This produces an .msi file which is then used to install the QBSDK Merge Module.